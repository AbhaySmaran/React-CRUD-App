This is a simple crud application using only react js.

To start this app first go inside the project directry then install dependencies then run.

follow the commands. 
-cd crud-app 
-npm install 
-npm run dev
